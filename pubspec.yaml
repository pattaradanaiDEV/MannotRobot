name: man_not_robot
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.8 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  go_router: ^13.2.0
  firebase_core: ^4.4.0
  cloud_firestore: ^6.1.2 
  firebase_auth: ^6.1.4
  firebase_ui_auth: ^3.0.1
  image_picker: ^1.2.1
  firebase_storage: ^13.1.0
  http: ^1.6.0
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/mock_recipes.json
    - assets/mock_jobs.json
